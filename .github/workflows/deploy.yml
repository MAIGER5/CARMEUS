name: Deploy to Hostingar via FTP

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Build the project
      run: npm run build

    - name: Deploy to Hostingar via FTP
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        ftp-server: ftp.maigerprofile.com	
        ftp-username: u347257743.maiger5	
        ftp-password: Linda4785
        local-dir: ./
        server-dir: /home/u347257743/domains/maigerprofile.com/public_html/carmeuseColombia	